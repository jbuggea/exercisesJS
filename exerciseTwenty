function pos_neg(x, y)
{
  if ((x < 0 && y > 0) || x > 0 && y < 0) 
  {
    return true;
  }
  else 
  {
    return false;
  }
}

console.log(pos_neg(2, -9));
console.log(pos_neg(-9, 3));
console.log(pos_neg(2, 9));
